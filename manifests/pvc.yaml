apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tagalong-data
  namespace: tagalong
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
